apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: audit-service-config
spec:
  tracing:
    samplingRate: '1'
    otel:
      endpointAddress: 'localhost:4318'
      isSecure: false
      protocol: 'http'
  metric:
    enabled: true
  accessControl:
    defaultAction: allow
    trustDomain: 'public'
